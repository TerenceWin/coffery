services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: coffery
      POSTGRES_PASSWORD: coffery_secret
      POSTGRES_DB: coffee_shop
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
